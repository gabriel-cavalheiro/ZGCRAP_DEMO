projection;
strict ( 1 );
use draft;

define behavior for ZC_GCRAP_CERTIFPRODUCT alias Certificate
{
  use create;
  use update;
  use delete;

  use association _Status { create; with draft;}

  use action Edit;
  use action Activate;
  use action Discard;
  use action Prepare;
  use action Resume;

}

define behavior for ZC_GCRAP_CERTIFSTPRODUCT alias CertificateST
{
  use delete;

  use association _Certif;
}